const expect = require('chai').expect;

const mongoose = require('mongoose');
const ioredis = require('ioredis');

const GoRedis = require('./index')(mongoose, ioredis);

describe('GoRedis', () => {

  it('should run this test', () => {






  });

});
